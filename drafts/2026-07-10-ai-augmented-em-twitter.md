1/
We turned a 3-day migration into an afternoon.

Not by writing faster code. By changing who writes the first draft.

2/
Pointed Claude Code at the legacy service, gave it the target schema + known edge cases. It drafted the migration scripts, backfill logic, and a first pass at tests.

Two engineers reviewed, caught a few wrong assumptions on nullable fields, shipped by EOD.

3/
The time saved wasn't the interesting part.

It's where the attention went. Engineers spent the day on the 2 decisions that actually mattered (rollback strategy, backfill-in-place vs write-forward) instead of boilerplate.

4/
As an EM this is changing how I plan capacity.

The bottleneck was never "can we code this." It was grunt work eating the hours where judgment happens. Agents don't replace judgment — they buy back the hours to use it.

5/
Still reviewing every diff. Still early.

But the shape of the job is shifting — and the teams that learn to delegate the right 70% to an agent are going to move at a different clock speed.

What's the first agent workflow that actually stuck for you?
