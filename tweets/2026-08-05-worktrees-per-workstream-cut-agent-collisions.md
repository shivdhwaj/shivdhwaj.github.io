# Worktrees per workstream cut agent collisions

**Angle:** Tool-specific insight

Started running Claude Code in separate worktrees per workstream instead of one shared branch. Fewer collisions between parallel agent runs, and I can review each thread's diff independently before anything touches main. Small change, real drop in review friction.
