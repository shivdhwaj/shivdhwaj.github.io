# Giving Claude Code a scratch repo changed how it debugs

**Angle:** Tool-specific insight

Pointing Claude Code at a disposable worktree to reproduce a bug before touching the real branch changed everything. It tests hypotheses instead of guessing. Bugs get isolated before a single line changes on main.
